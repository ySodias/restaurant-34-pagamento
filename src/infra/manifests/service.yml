apiVersion: v1
kind: Service
metadata:
  name: restaurant-34-pagamento
spec:
  type: LoadBalancer
  selector:
    app: restaurant-34-pagamento
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000